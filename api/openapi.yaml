openapi: 3.1.0
info:
  title: Pantry API
  version: 0.1.0
  description: |
    Foundation contract for the staged Go port. Only health, readiness, and
    verified-identity endpoints are implemented in this revision. Household,
    recipe, shopping, and scrape capabilities remain on their existing client
    paths until each has passed the compatibility gates in contracts/compatibility.md.
paths:
  /healthz:
    get:
      responses:
        '200':
          description: Process is live.
  /readyz:
    get:
      responses:
        '200':
          description: Foundation process is configured and accepting traffic.
  /api/v1/whoami:
    get:
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Verified caller identity.
        '401':
          $ref: '#/components/responses/Unauthenticated'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    Unauthenticated:
      description: Missing, invalid, expired, or wrong-role access token.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Problem'
  schemas:
    Problem:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
