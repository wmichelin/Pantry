EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# DATABASE_URL= — optional, for local pg_dump / scripts/scheduled-do-pg-backup.sh only; paste from Supabase Dashboard → Database URI (never commit real values)
